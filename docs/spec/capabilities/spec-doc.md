<!-- spec-doc: сгенерировано из названий тестов, руками не править -->
# spec-doc

Скрипт `spec-doc` скилла `spec`: документация `docs/spec` из тестов — страница на capability и стандарт,
которая читается рассказом: зачем, что умеет, чем проверено.

Требование существует, пока есть проверяющий его тест, поэтому документацию о поведении не пишут руками: её
собирают из отчётов раннеров и JSDoc тестов, и с тестами она не расходится. `docs/spec` коммитится вместе с
PR, CI проверяет, что она не отстала.

## Страница capability — рассказ: зачем, что умеет, чем проверено

Зачем — шапка главного файла папки, что умеет — разделы из `describe`, чем проверено — тесты, свёрнутые под
разделом: доказательства не заслоняют рассказ.

<details><summary>✅ 8 тестов</summary>

- ✅ шапка главного файла `<name>.test.ts` — абзацы под заголовком, первый — описание в оглавлении
- ✅ describe — раздел с прозой из JSDoc, вложенный describe — на уровень глубже
- ✅ тесты раздела свёрнуты под счётчиком, JSDoc теста — цитата под его строкой
- ✅ есть падающие или пропущенные — они в строке-заголовке, и блок раскрыт
- ✅ тест без describe — сразу под заголовком capability
- ✅ разделы — в порядке главного файла, остальные файлы — следом по пути
- ✅ один describe из нескольких файлов — один раздел, файлы по порядку путей
- ✅ у стандарта главный файл — rule.test.ts: его шапка и его тесты первыми

</details>

## В документацию идёт только проза для читателя спеки

Отчёты раннеров комментариев не несут — прозу `spec-doc` берёт из исходников тестов тем же сканером, что
`spec-diff`. JSDoc — для читателя спеки; причины решений, устройство теста и ссылки на задачи — `//`, для
читателя кода.

<details><summary>✅ 4 теста</summary>

- ✅ шапка не в главном файле папки в документацию не идёт — spec-doc называет файл, --strict — код 1
- ✅ `//`-комментарий, блочные теги JSDoc и JSDoc, отделённый от вызова кодом, в документацию не попадают
- ✅ JSDoc вплотную к describe в файле без импортов — проза describe, а не capability
- ✅ нет исходника (отчёт с другой машины) — документация без прозы, не ошибка

</details>

## Пропущенный и падающий тест видны — на странице и в оглавлении

<details><summary>✅ 2 теста</summary>

- ✅ пропущенный, упавший и todo помечены своим статусом
- ✅ в оглавлении у capability — только счётчики пропущенных и падающих, общего числа тестов нет

</details>

## Отчёты Vitest, Playwright и bun test — в любом сочетании

<details><summary>✅ 6 тестов</summary>

- ✅ Vitest: абсолютный путь с другой машины приводится по сегменту /tests/
- ✅ Playwright: проекты сворачиваются в одну строку, причина skip — из аннотации, худший статус побеждает
- ✅ bun test: describe — вложенные testsuite JUnit, todo и skip различаются
- ✅ одинаковый тест из двух отчётов — одна строка с худшим статусом
- ✅ неизвестный формат — код 2 и сообщение
- ✅ без отчётов — код 2 и подсказка

</details>

## Оглавление: что делает система, как построена и что без дома

`tests/lib` — фабрики и хелперы, не спека. Тест вне `capabilities/<name>` и `standards/<name>` — без дома:
это сигнал перенести, а не ошибка разбора.

<details><summary>✅ 4 теста</summary>

- ✅ capability — в «Что делает система», стандарт — в «Как построена»
- ✅ tests/lib пропускается, тесты вне дерева — в раздел «Вне дерева»
- ✅ --strict возвращает 1 при тестах вне дерева, без него — 0
- ✅ без тестов вне дерева раздела нет

</details>

## docs/spec обновляется сам и не трогает рукописное

Маркер в первой строке отличает сгенерированный файл от рукописного: свои устаревшие файлы скрипт удаляет,
чужие не трогает.

<details><summary>✅ 3 теста</summary>

- ✅ пишет README.md, capabilities/\<name>.md и standards/\<name>.md с маркером
- ✅ удаляет свой устаревший файл и не трогает чужой
- ✅ --out задаёт каталог

</details>

## Названия — текст, а не разметка

Название теста — текст, а не разметка: его куски не должны пропадать при показе на GitHub.

<details><summary>✅ 1 тест</summary>

- ✅ `<` вне code span экранируется — `<type>` и `<!-- … -->` не пропадают как HTML, в code span — как есть

</details>
