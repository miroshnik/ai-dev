<!-- spec-doc: сгенерировано из названий тестов, руками не править -->
# spec-diff

Скрипт `spec-diff` скилла `spec`: ревьюер PR видит, какие требования PR снимает, меняет и добавляет, —
раздел «Спека (тесты)» для тела PR с названиями тестов.

Дифф тестов — дифф требований: удалённый тест — снятое требование, и оно не должно пройти незамеченным среди
сотен строк кода. Названия берутся статическим разбором исходников на двух ревизиях — без прогона тестов, за
секунды и в CI без установки зависимостей.

## Снятые требования — первыми, потом изменённые и добавленные

<details><summary>✅ 4 теста</summary>

- ✅ удалённые идут первыми, потом изменённые, потом добавленные
- ✅ без изменений — «Тесты не менялись»
- ✅ `<` в названиях вне code span экранируется — GitHub не съест `<type>` в теле PR
- ✅ пустой список — «нет», а не пропуск раздела

</details>

## Изменён — только очевидное переименование, иначе удалён и добавлен

Переименованный тест ищется только в том же файле и том же describe (похожесть имени ≥ 0,6); тест с тем же
именем под другим describe — в любом файле папки. Порог высокий: спрятать снятое требование под видом
переименования хуже, чем показать переименование как удаление и добавление.

<details><summary>✅ 4 теста</summary>

- ✅ непохожее имя в том же describe — удалён и добавлен, не переименование
- ✅ переименован describe — изменён, старый describe зачёркнут
- ✅ тот же тест под другим describe в другом файле папки — изменён: переехал в другой раздел
- ✅ переименование в другом файле — не изменение, а удалён и добавлен

</details>

## Тест — это папка, цепочка describe и имя: перекладка между файлами папки — не изменение

Файл в идентичность не входит: единица спеки — папка capability, тесты внутри неё можно перекладывать между
файлами.

<details><summary>✅ 3 теста</summary>

- ✅ перенос между файлами одной папки — не изменение
- ✅ перенос в другую capability — удалён и добавлен
- ✅ тест в tests/, но вне capabilities/standards — помечен «вне дерева»

</details>

## Сравнение — с тем, что меняет PR, как дифф на GitHub

По умолчанию база — merge-base с базовой веткой, как дифф PR на GitHub: чужие тесты, влитые в `main` после
ветвления, не выглядят удалёнными.

<details><summary>✅ 4 теста</summary>

- ✅ merge-base: тест, добавленный в main после ветвления, не считается удалённым
- ✅ --no-merge-base сравнивает с веткой как есть
- ✅ --worktree видит незакоммиченные и неотслеживаемые тесты
- ✅ нет такой ревизии — код 2 и подсказка про fetch

</details>

## Изменённые тесты вне дерева видны по имени файла

<details><summary>✅ 2 теста</summary>

- ✅ изменённый файл теста вне tests/ перечисляется по имени
- ✅ tests/lib не участвует

</details>

## Переезд тестов в дерево — сводкой по папкам, а не тысячами «добавлен»

Проект, который переходит на тест-спек, переносит тесты в дерево: на базе их в `tests/` нет, и без сопоставления
каждый выглядел бы новым требованием, а PR с тысячами тестов не влез бы в тело PR. Тест, который на базе жил в
файле вне дерева и там исчез, а в дереве появился с той же цепочкой describe и именем, — перенесён.

<details><summary>✅ 5 тестов</summary>

- ✅ тест с тем же describe и именем из файла вне дерева — перенесён, а не добавлен; сводка по папкам
- ✅ тест, переименованный при переносе, — добавлен, а файл-источник — в списке вне дерева
- ✅ одинаковые тесты двух файлов, перенесённые в одну папку, — оба перенесены, файлы не по имени
- ✅ тест, оставшийся и вне дерева, — копия: в дереве он добавлен
- ✅ --worktree: перенос до коммита тоже сворачивается

</details>

## --json — то же для машины

<details><summary>✅ 2 теста</summary>

- ✅ перенесённые — с исходным и новым путём
- ✅ машинный формат с тремя списками и файлами вне дерева

</details>

## Названия берутся из исходников сканером — без прогона и без парсера

Парсер TypeScript — зависимость, прогон — минуты, поэтому имя теста — строковый литерал первым аргументом
`describe` / `it` / `test`. Динамическое имя видно с плейсхолдером (`it.each`) или не видно вовсе.

<details><summary>✅ 8 тестов</summary>

- ✅ вложенные describe дают цепочку, объект опций перед колбэком не считается телом
- ✅ test.describe Playwright и модификаторы skip/only/serial/todo/fixme
- ✅ it.each и describe.each — имя второго вызова с плейсхолдером
- ✅ комментарии и строки не считаются вызовами
- ✅ test.step, test.use, beforeEach и test.fail() без имени — не тесты
- ✅ шаблонная строка с подстановкой сохраняется как есть, экранирование снимается
- ✅ xit / xdescribe / fit Jest и suite / context Mocha
- ✅ describe с колбэком-переменной не открывает вложенность

</details>
