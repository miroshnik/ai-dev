<!-- spec-doc: сгенерировано из названий тестов, руками не править -->
# est

Скилл `est`: сколько часов работы агента займёт задача — по фактам похожих закрытых задач, и сколько заняла на
самом деле — часы, токены и стоимость из транскриптов.

Оценка «из головы» ничем не проверяема, поэтому и оценка, и факт здесь измеряются: факт — по сессиям агента,
привязанным к задаче, оценка — по фактам аналогов. Агент выбирает аналоги и объясняет расхождения; скрипт
`est.ts` делает детерминированное — читает транскрипты Claude Code, Codex и облачных сессий, ставит поля
проекта и пишет комментарии «Оценка» и «Факт».

## Факт — активные часы работы над задачей по её сессиям

Факт — активные часы: записи, привязанные к задаче, и паузы между ними не длиннее gap (30 мин); общий PR на
несколько задач делится поровну.

<details><summary>✅ 4 теста</summary>

- ✅ записи на ветке задачи и субагента с подсказкой считаются, паузы ≤ gap — время, токены и цена — с долей
- ✅ общий PR на две задачи — доля ½ на записях ветки, субагент с подсказкой — целиком; в результате есть «shared»
- ✅ сессия на чужой ветке не привязывается — покрытие none, факт недоступен
- ✅ пауза длиннее gap не входит во время

</details>

## Сессия, которая ведёт задачи подряд, делит время между ними

Одна сессия может вести несколько задач подряд: её переименовывают под каждую следующую. Записи на ветке без
номера задачи (её назвал инструмент worktree) достаются задаче из названия сессии на момент записи: первое
название действует с начала сессии, каждое следующее — с переименования. Одна запись — одной задаче.

<details><summary>✅ 7 тестов</summary>

- ✅ записи безномерной ветки достаются задаче из названия на момент записи, а не из последнего; ни одна не засчитана двум задачам
- ✅ pr-link смёрженного PR прошлой задачи, повторённый после мержа, не обрывает время следующей задачи
- ✅ окно своего PR не тянется назад через переименование: записи на HEAD под прошлым названием остаются прошлой задаче
- ✅ маркер факта хранит интервалы по сессиям — по ним следующий расчёт видит, что уже засчитано
- ✅ запись, уже засчитанная в записанном факте другой задачи той же сессии, по названию повторно не засчитывается — комментарий называет ту задачу
- ✅ повтор той же записи названия не начинает новый период — переход на другую ветку без номера по-прежнему закрывает окно
- ✅ запись на своей ветке остаётся своей и при пересечении с записанным фактом другой задачи — только предупреждение

</details>

## PR достаётся задаче, которую он делает, а не той, что упомянута

PR задачи: сильные связи (закрыл, `Closes #N`, номер в ветке, связан вручную) и — только если сильных нет —
слабые, упоминания. Упоминание в PR, который закрывает другие задачи, — не работа над этой: иначе задача без
своего PR получает чужой PR, его время и тип.

<details><summary>✅ 3 теста</summary>

- ✅ PR закрывает другую задачу и лишь упоминает эту — к ней не привязывается
- ✅ PR без закрываемых задач, упоминающий задачу, — слабая связь, когда сильных нет
- ✅ сильная связь — Closes #N или номер в ветке — важнее упоминаний

</details>

## Ветка `<type>/<issue>-<slug>` даёт номер и тип задачи

Номер привязывает работу к задаче, тип нужен истории оценок — аналоги ищутся среди задач того же типа.

<details><summary>✅ 2 теста</summary>

- ✅ номер — отдельный токен ветки, даты и однозначные числа без issue- не считаются
- ✅ конвенция \<type>/N-slug даёт номер и тип, префикс области допустим

</details>

## Транскрипт Claude Code даёт привязки к задаче и расход токенов

Из транскрипта берутся привязки к задаче — название сессии, промпты, ветка, хеши коммитов, PR — и расход
токенов.

<details><summary>✅ 2 теста</summary>

- ✅ название сессии, первый промпт, промпты, хеши из вывода инструментов, pr-link, usage раз на ответ, подсказка субагента
- ✅ вставки \<system-reminder> перед текстом промпта вырезаются: промпт человеческий, номер задачи — из текста, а не из вставки; одна вставка — не промпт

</details>

## Транскрипт Codex — такой же источник факта, только без ветки

У записей Codex нет ветки — привязка к задаче только по номеру в промпте и хешам коммитов.

<details><summary>✅ 1 тест</summary>

- ✅ cwd и id из session_meta, промпт из UserMessage, токены раз на response_id, хеши из выводов инструментов

</details>

## Облачная сессия считается по событиям, выгруженным из claude.ai

Облачная сессия Claude Code (claude.ai/code): её транскрипт — события сессии, выгруженные из claude.ai браузером
(`est cloud-import`). Это ещё один источник факта, как Codex: те же записи, ветки, промпты, токены и хеши, только
ветка приходит событием `vcs_state_changed`, а человек — это `source: client`. Коммит PR с трейлером
`Claude-Session`, чья сессия не импортирована, — покрытие partial со ссылкой на сессию: часть работы не видна.

<details><summary>✅ 6 тестов</summary>

- ✅ события → сессия: человек — клиент, ветка — из vcs_state_changed и вывода git (до них нейтральная), токены раз на ответ, хеши из выводов, субагент — в таймлайне
- ✅ облачная сессия считается в факт наравне с локальной — покрытие full, агент назван
- ✅ коммит PR с трейлером Claude-Session, чья сессия не импортирована, — покрытие partial и ссылка на сессию
- ✅ ключ облачной сессии в маркере — после префикса session_, локальной — как был
- ✅ трейлер Claude-Session читается из тела коммита PR
- ✅ est cloud-import кладёт выгрузку в личный каталог по репозиторию; не выгрузка — ошибка

</details>

### облачная часть в маркере факта

Облачная сессия считает свою часть сама — по своему транскрипту в контейнере — и отдаёт её в комментарии «Факт
(облако)» маркером `cloud`. Поля проекта из облака не поставить: их ставит локальный `est fact --write` / `--sweep`,
складывая облачную часть с локальной. Выгрузка той же сессии, если импортирована, важнее части.

<details><summary>✅ 4 теста</summary>

- ✅ облачная часть считается, как сессия: время, токены, стоимость, покрытие full по трейлеру; в маркере — сохраняется
- ✅ с локальной работой той же поры — сумма без двойного счёта пересечения
- ✅ выгрузка той же сессии импортирована — часть из маркера второй раз не считается
- ✅ части собираются из всех комментариев с маркером факта; по сессии — последняя

</details>

## В облачной сессии est считает свою часть факта, остальное — ошибка с объяснением

В облачной сессии Claude Code (claude.ai/code) GitHub доступен только через REST своего репозитория: GraphQL и
проекты закрыты. Факт своей сессии est считает по транскрипту в контейнере и печатает готовый комментарий —
агент записывает его инструментом GitHub, поля проекта ставит ближайшая локальная сессия.

<details><summary>✅ 3 теста</summary>

- ✅ est fact печатает комментарий «Факт недоступен (облако)» со ссылкой на сессию и маркером факта — в GitHub не ходит
- ✅ est fact в облаке считает часть сессии по её транскрипту в контейнере: «Факт (облако)» с цифрами и маркером части
- ✅ оценка, история и sweep в облаке — ошибка с объяснением, а не сбой gh

</details>

## Комментарии «Оценка» и «Факт» обновляются на месте, строки человека сохраняются

Комментарии «Оценка» и «Факт» кончаются машинным маркером `<!-- est {…} -->` или `<!-- fact {…} -->`: по
нему скрипт находит свой комментарий и обновляет его, а не пишет новый. Строки, дописанные человеком,
переживают перезапись.

<details><summary>✅ 4 теста</summary>

- ✅ маркер \<!-- est {…} --> читается, битый JSON — как отсутствие
- ✅ «+ вручную: N ч» и «Причина: …» сохраняются при перезаписи комментария
- ✅ текст с часами, отношением к оценке, PR и диффом; маркер разбирается обратно; строки человека сохранены
- ✅ без сессий — «Факт недоступен» и покрытие none в маркере

</details>

## Стоимость — API-эквивалент по публичным тарифам

На подписке эти деньги не списываются, но стоимость сравнима между задачами и моделями.

<details><summary>✅ 2 теста</summary>

- ✅ вход, выход, запись кэша ×1.25 (5 мин) и ×2 (1 ч), чтение кэша — по тарифу модели
- ✅ модель вне прайса — без цены (null), а не ноль

</details>

## Оценка — ступень шкалы от 0,1 до 13 ч

Ступени 0,1 · 0,25 · 0,5 · 1 · 1,5 · 2 · 3 · 5 · 8 · 13 ч: точнее по аналогам не угадать, а больше 13 ч — задачу надо дробить.

<details><summary>✅ 1 тест</summary>

- ✅ округляет к ближайшей ступени, при равном расстоянии — к меньшей, выше 13 не бывает

</details>

## Калибровка k показывает, сходятся ли оценки с фактами

k — отношение факт/оценка по истории; справочное — к прогнозу по аналогам не применяется.

<details><summary>✅ 1 тест</summary>

- ✅ k — медиана факт/оценка по задачам с маркером и полным покрытием; partial не в счёт

</details>

## Время — без двойного счёта, коммит узнаётся и по короткому хешу

Время — объединение интервалов активности; хеш коммита в выводе инструментов бывает коротким или полным.

<details><summary>✅ 1 тест</summary>

- ✅ пересекающиеся интервалы сливаются, короткий хеш — префикс полного

</details>

## Вывод читается человеком: часы без хвостовых нулей, склонения, периоды

<details><summary>✅ 2 теста</summary>

- ✅ часы без хвостовых нулей, отсутствие — тире, склонение по числу
- ✅ период --since: дни, недели, месяцы, часы; иное — ошибка

</details>

## Неверный вызов — справка или ошибка до обращения к GitHub

<details><summary>✅ 2 теста</summary>

- ✅ без команды — справка и код 2, неизвестная команда — ошибка
- ✅ проверки аргументов — до обращения к GitHub

</details>
